# Pinned to latest stable versions as of 2026-02-25
# Run: pip install -r requirements.txt && playwright install chromium

playwright==1.58.0       # Microsoft browser automation; CVE-2025-59288 fixed in 1.55.1+
requests==2.32.5         # HTTP client
beautifulsoup4==4.14.3   # HTML parsing
lxml==6.0.2              # XML/HTML parser (used by bs4 for sitemap parsing)
# Note: feedparser removed — RSS/Atom parsing uses stdlib xml.etree.ElementTree
